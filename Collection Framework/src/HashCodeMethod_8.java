
// need to see day 14 in Arjun's repo
public class HashCodeMethod_8 {
// o1,o2 ==> address
    // int hashCode (Object object) // aka hashing function.
    //note --->hashcode method takes object as argument and returns integer.
    // note completely true: which integer it will return will be the objects address in the memory.
    public static void main(String[] args) {

        Integer num=45;
        num.hashCode();   // for example, it will return some int
    }
}
